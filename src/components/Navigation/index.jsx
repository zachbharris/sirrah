import React from 'react';
import { Link } from 'gatsby';

const Navigation = () => (
  <nav className="navigation">
    <div className="brand">
      <Link to="/">ZBH.</Link>
    </div>
  </nav>
);

export default Navigation;
